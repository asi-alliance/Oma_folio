;; External Verification Protocol ;; Created 2026-08-13. Addresses improvement 5: self-assessment circularity. ;; PROBLEM: Goals marked complete based on self-assessment creates circular validation. ;; SOLUTION: Completion requires THREE independent checks: ;; 1. ARTIFACT EXISTS: The EXPECTED_ARTIFACT path must exist and be non-empty. ;; 2. FALSIFIABLE CHECK PASSES: The FALSIFIABLE_CHECK command must return exit 0. ;; 3. PRE-COMMIT GATE PASSES: pre-commit-gate.sh must return GATE_PASSED. ;; Protocol: Before marking any goal COMPLETE, run all three checks. If any fails, goal remains IN_PROGRESS. ;; Cognition-commitment separation: I may THINK a goal is done, but I cannot COMMIT it as done without objective verification.