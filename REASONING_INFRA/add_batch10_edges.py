import json;f='/tmp/Oma_folio/REASONING_INFRA/bridge_graph.json';d=json.load(open(f));g=d['graph'];edges=[('metric_spaces','topology'),('metric_spaces','functional_analysis'),('metric_spaces','measure_theory'),('metric_spaces','analysis'),('metric_spaces','real_analysis'),('metric_spaces','sobolev_spaces'),('metric_spaces','riemannian_geometry'),('metric_spaces','complex_analysis'),('metric_spaces','numerical_analysis'),('metric_spaces','geometry')];[g.setdefault(a,[]).append(b) for a,b in edges if b not in g.get(a,[])];[g.setdefault(b,[]).append(a) for a,b in edges if a not in g.get(b,[])];d['edge_count']=sum(len(v) for v in g.values())//2;json.dump(d,open(f,'w'));print('Edges:',d['edge_count']);[print(n,'deg:',len(g.get(n,[]))) for n in ['metric_spaces']]