BRIDGE-CENTRALITY IMPORTANCE DIMENSION DESIGN. PURPOSE: Measure how strongly a concept connects ACROSS distinct fields, not just within-field degree. Fields merge through shared rules of nature, manufacturing, and applied technology. FORMULA: bridge_score(concept) = count(distinct_field_clusters(neighbors)) / total_field_clusters. CLUSTERS: Algebra, Topology, Analysis, Geometry, Physics, CS_Logic, Applied. ANNOTATION FORMAT: IMPORTANCE-ANNOTATION pipe concept pipe score 1-10 pipe dimension pipe reason pipe source. Add dimension=bridge-centrality with computed cross-cluster score.