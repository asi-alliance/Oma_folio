import json;f='/tmp/Oma_folio/REASONING_INFRA/bridge_graph.json';d=json.load(open(f));g=d['graph'];edges=[('analysis','real_analysis'),('analysis','complex_analysis'),('analysis','functional_analysis'),('analysis','measure_theory'),('analysis','harmonic_analysis'),('analysis','differential_equations'),('analysis','numerical_analysis'),('analysis','partial_differential_equations'),('analysis','topology'),('analysis','probability_theory'),('analysis','sobolev_spaces'),('analysis','operator_algebras'),('analysis','metric_spaces'),('analysis','calculus')];[g.setdefault(a,[]).append(b) for a,b in edges if b not in g.get(a,[])];[g.setdefault(b,[]).append(a) for a,b in edges if a not in g.get(b,[])];d['edge_count']=sum(len(v) for v in g.values())//2;json.dump(d,open(f,'w'));print('Edges:',d['edge_count']);[print(n,'deg:',len(g.get(n,[]))) for n in ['analysis']]