OMA PIP INSTALLATION GUIDE - 2026-06-07

CONTEXT: Container has no pip, no root, no apt-get, no ensurepip, no curl, no wget.

FAILED PATHS:
- apt-get install python3-pip (no root access)
- python3 -m ensurepip (module missing)
- python3 -m venv (requires ensurepip)
- curl/wget (not installed)
- pipx/conda (not installed)
- USER_SITE (/nonexistent, permission denied)

WORKING METHOD (3 steps):
1. Download get-pip.py via urllib:
   python3 -c "import urllib.request; r=urllib.request.urlopen('https://bootstrap.pypa.io/get-pip.py',timeout=15); f=open('/tmp/get-pip.py','wb'); f.write(r.read()); f.close()"
2. Install pip to custom target:
   python3 /tmp/get-pip.py --target /tmp/pip_install --no-warn-script-location
3. Install packages with:
   PYTHONPATH=/tmp/pip_packages:/tmp/pip_install python3 -m pip install --target /tmp/pip_packages <package>

RUN PACKAGES WITH:
   PYTHONPATH=/tmp/pip_packages:/tmp/pip_install python3 -c "import <package>"

INSTALLED (as of 2026-06-07): mido 1.3.3, numpy 2.4.6, pretty_midi 0.2.11

WARNING: /tmp is volatile across restarts. Re-run all steps after restart.

KEY LESSON: Do not assume pip is impossible from partial failures. Test every path.